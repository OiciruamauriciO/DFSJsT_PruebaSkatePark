    <h1>{{loginTitle}}</h1>

    <div class="py-5">
      <h2>{{loginSubTitle}}</h2>
      <hr class="w-50" />

      <form method="post" action="/login">
        <div class="form-group">
          <div class="form-group">
            <label>Email</label>
            <input id="email" name="email" class="form-control w-50 m-auto" />
          </div>
          <div class="form-group">
            <label>Password</label>
            <input type="password" id="password" name="password" class="form-control w-50 m-auto" />
          </div>
        </div>
        <button class="btn btn-success mb-3" type="submit">Ingresar</button>
        <p>{{footerMessage}}<a href="/register">Regístrate</a></p>
      </form>
    </div>